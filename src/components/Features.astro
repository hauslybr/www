---
const features = [
  {
    icon: `<svg width="28" height="28" viewBox="0 0 28 28" fill="none"><rect x="2" y="4" width="24" height="20" rx="4" stroke="currentColor" stroke-width="1.5"/><path d="M2 10h24" stroke="currentColor" stroke-width="1.5"/><circle cx="7" cy="7" r="1" fill="currentColor"/><circle cx="11" cy="7" r="1" fill="currentColor"/></svg>`,
    title: 'Use com uma mão, na rua',
    description: 'Interface pensada para o corretor entre visitas. Menos clique, menos tela, mais ação concreta direto do celular.',
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 28 28" fill="none"><path d="M14 3l3 6 7 1-5 5 1 7-6-3-6 3 1-7-5-5 7-1 3-6z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/></svg>`,
    title: 'Saiba qual lead priorizar hoje',
    description: 'A Hausly mostra quem está quente e quem precisa de ação. Foco no que realmente fecha negócio.',
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="11" stroke="currentColor" stroke-width="1.5"/><path d="M14 8v6l4 3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>`,
    title: 'Suas visitas nunca mais furam',
    description: 'Agenda sincronizada com Google Calendar, confirmação automática e lembrete para você e para o cliente.',
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 28 28" fill="none"><path d="M4 14l7 7L24 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>`,
    title: 'Zero lead sem próxima ação',
    description: 'Nenhum contato fica parado. O sistema garante que todo lead tem um próximo passo definido.',
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 28 28" fill="none"><rect x="3" y="3" width="10" height="10" rx="3" stroke="currentColor" stroke-width="1.5"/><rect x="15" y="3" width="10" height="10" rx="3" stroke="currentColor" stroke-width="1.5"/><rect x="3" y="15" width="10" height="10" rx="3" stroke="currentColor" stroke-width="1.5"/><rect x="15" y="15" width="10" height="10" rx="3" stroke="currentColor" stroke-width="1.5"/></svg>`,
    title: 'Carteira organizada e conectada',
    description: 'Cadastre imóveis e vincule aos leads interessados. Contexto completo em cada negociação, sem retrabalho.',
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 28 28" fill="none"><path d="M14 3v22M3 14h22" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><circle cx="14" cy="14" r="4" stroke="currentColor" stroke-width="1.5"/></svg>`,
    title: 'Pipeline visual do funil inteiro',
    description: 'Veja cada oportunidade do primeiro contato ao fechamento. Saiba quanto tem no funil e onde agir.',
  },
];
---

<section class="section features">
  <div class="container">
    <div class="features__header reveal">
      <span class="kicker">Benefícios</span>
      <h2>O que muda no seu dia a dia<br />com a Hausly</h2>
    </div>

    <div class="features__grid">
      {features.map((feature, i) => (
        <article class={`features__card reveal reveal-delay-${(i % 3) + 1}`}>
          <div class="features__card-icon" set:html={feature.icon} />
          <h3>{feature.title}</h3>
          <p>{feature.description}</p>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .features {
    background: var(--white);
    border-top: 1px solid var(--border);
  }

  .features__header {
    max-width: 540px;
  }

  .features__grid {
    margin-top: var(--space-xl);
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }

  .features__card {
    padding: var(--space-lg);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--surface);
    transition:
      transform var(--duration-md) var(--ease-out),
      border-color var(--duration-md),
      box-shadow var(--duration-md) var(--ease-out);
  }

  .features__card:hover {
    transform: translateY(-4px);
    border-color: var(--amber);
    box-shadow:
      var(--shadow-lg),
      0 0 0 1px rgba(221, 148, 41, 0.1);
  }

  .features__card-icon {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-md);
    background: var(--amber-glow);
    color: var(--amber);
    margin-bottom: var(--space-md);
    transition: background var(--duration-md);
  }

  .features__card:hover .features__card-icon {
    background: rgba(221, 148, 41, 0.2);
  }

  .features__card h3 {
    font-family: var(--font-body);
    font-weight: 800;
    font-size: 1.05rem;
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
    line-height: 1.3;
  }

  .features__card p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.65;
  }

  @media (max-width: 768px) {
    .features__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
