---
const pains = [
  {
    icon: '💬',
    title: 'O lead de sábado esfriou na segunda',
    description: 'Chegou no fim de semana, você não anotou na hora e na segunda-feira ele já não responde. Escapou pelo WhatsApp direto pro seu concorrente.',
  },
  {
    icon: '🚪',
    title: 'A visita que furou (de novo)',
    description: 'Você separou a chave, desmarcou outro compromisso e o cliente não apareceu. Sem confirmação automática, sem aviso.',
  },
  {
    icon: '📊',
    title: 'A planilha que ninguém atualiza',
    description: 'Você tenta usar Excel, mas depois das 10h da manhã a correria engole a rotina. A planilha vira um cemitério de contatos mortos.',
  },
  {
    icon: '🧠',
    title: 'A carga mental de "lembrar de tudo"',
    description: 'Aquela sensação horrível de no fim do dia sentir que esqueceu de retornar para alguém importante, mas não sabe para quem.',
  },
];
---

<section id="problema" class="section pain">
  <div class="container">
    <div class="pain__intro reveal">
      <div class="pain__intro-text">
        <span class="kicker kicker--red">A dura realidade</span>
        <h2>Você já perdeu negócio assim.</h2>
        <p class="pain__lead">
          O corretor autônomo não perde venda por falta de lábia. Perde venda por <strong>desorganização.</strong> Cada lead que esfria no WhatsApp é dinheiro que você deixou na mesa.
        </p>
      </div>
      <div class="pain__intro-image reveal delay-1">
        <img
          src="/pain-desk.webp"
          alt="Corretor frustrado olhando para anotações bagunçadas"
          width="1264"
          height="848"
          loading="lazy"
        />
        <div class="pain__image-overlay">
           <div class="status-pill">
              <span class="dot"></span>
              Lead arquivado: 4 dias atrás
           </div>
        </div>
      </div>
    </div>

    <div class="pain__grid">
      {pains.map((pain, i) => (
        <article class={`pain__card reveal delay-${(i % 4) + 1}`}>
          <div class="pain__card-icon">{pain.icon}</div>
          <h3>{pain.title}</h3>
          <p>{pain.description}</p>
          <div class="pain__card-line"></div>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .pain {
    background: var(--surface);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }

  .kicker--red {
    color: var(--red-500);
  }

  .kicker--red::before {
    background: var(--red-500);
  }

  .pain__intro {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl);
    align-items: center;
  }

  .pain__intro-text h2 {
    color: var(--text-pure);
    letter-spacing: -0.03em;
  }

  .pain__lead {
    margin-top: var(--space-md);
    font-size: 1.15rem;
    line-height: 1.6;
    color: var(--text-secondary);
  }

  .pain__lead strong {
     color: var(--red-500);
  }

  .pain__intro-image {
    position: relative;
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-lg);
  }

  .pain__intro-image img {
    display: block;
    width: 100%;
    height: auto;
    object-fit: cover;
    filter: sepia(0.2) contrast(1.1);
  }

  .pain__image-overlay {
     position: absolute;
     bottom: var(--space-md);
     left: var(--space-md);
  }

  .status-pill {
     display: flex;
     align-items: center;
     gap: 8px;
     background: rgba(30, 41, 59, 0.9);
     backdrop-filter: blur(8px);
     color: white;
     padding: 8px 16px;
     border-radius: var(--radius-full);
     font-size: 13px;
     font-weight: 600;
  }

  .status-pill .dot {
     width: 8px;
     height: 8px;
     background: var(--red-500);
     border-radius: 50%;
  }

  .pain__grid {
    margin-top: var(--space-2xl);
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-lg);
  }

  .pain__card {
    position: relative;
    padding: var(--space-lg);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--white);
    overflow: hidden;
    transition:
      transform var(--duration-md) var(--ease-out),
      box-shadow var(--duration-md) var(--ease-out),
      border-color var(--duration-md) var(--ease-out);
  }

  .pain__card:hover {
    transform: translateY(-6px);
    box-shadow: 0 24px 48px rgba(239, 68, 68, 0.08); /* Red tint shadow */
    border-color: rgba(239, 68, 68, 0.2);
  }

  .pain__card-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background: var(--slate-100);
    border-radius: var(--radius-md);
    font-size: 1.5rem;
    margin-bottom: var(--space-md);
    transition: background var(--duration-md);
  }

  .pain__card:hover .pain__card-icon {
     background: var(--red-glow);
  }

  .pain__card h3 {
    font-family: var(--font-body);
    font-weight: 800;
    font-size: 1.25rem;
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
    line-height: 1.3;
  }

  .pain__card p {
    font-size: 15px;
    color: var(--text-secondary);
    line-height: 1.65;
  }

  .pain__card-line {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--red-500), var(--amber));
    opacity: 0;
    transition: opacity var(--duration-md);
  }

  .pain__card:hover .pain__card-line {
    opacity: 1;
  }

  @media (max-width: 900px) {
    .pain__intro {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }
  }

  @media (max-width: 768px) {
    .pain__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
