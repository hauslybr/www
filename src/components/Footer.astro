---
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="container footer__inner">
    <div class="footer__brand">
      <div class="footer__brand-row">
        <img src="/hausly-logo-dark.svg" alt="Hausly" width="32" height="32" class="footer__logo" />
        <span class="footer__wordmark">Hausly CRM</span>
      </div>
      <p class="footer__tagline">CRM agenda-first para corretores que vendem na rua.</p>
    </div>

    <div class="footer__links">
      <div class="footer__links-group">
        <h4>Produto</h4>
        <a href="/#como-funciona">Como funciona</a>
        <a href="/#preco">Preços</a>
        <a href="/#faq">FAQ</a>
      </div>
      <div class="footer__links-group">
        <h4>Contato</h4>
        <a href="mailto:contato@gtechgo.com">contato@gtechgo.com</a>
        <span class="footer__address">Av. Portugal, 1148 — Ed. Orion Business<br />Setor Marista · Goiânia, GO</span>
      </div>
      <div class="footer__links-group">
        <h4>Legal</h4>
        <a href="/politica-de-privacidade">Política de Privacidade</a>
        <a href="/exclusao-de-conta">Exclusão de Conta</a>
      </div>
    </div>
  </div>

  <div class="container footer__bottom">
    <div class="footer__bottom-row">
      <small>&copy; {currentYear} Hausly CRM — <strong>GT Tecnologia e Serviços LTDA</strong> · CNPJ 46.112.388/0001-13</small>
      <a href="/#inicio" class="footer__back-top">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M8 13V3m0 0L4 7m4-4l4 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        Topo
      </a>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--navy-950);
    color: var(--slate-400);
    padding: var(--space-2xl) 0 var(--space-lg);
    border-top: 1px solid var(--navy-700);
  }

  .footer__inner {
    display: flex;
    justify-content: space-between;
    gap: var(--space-2xl);
  }

  .footer__brand-row {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .footer__logo {
    width: 32px;
    height: 32px;
    border-radius: 8px;
  }

  .footer__wordmark {
    font-family: var(--font-display);
    font-size: 1.25rem;
    color: var(--white);
  }

  .footer__tagline {
    margin-top: var(--space-sm);
    font-size: 14px;
    color: var(--slate-500);
    max-width: 300px;
    line-height: 1.5;
  }

  .footer__links {
    display: flex;
    gap: var(--space-2xl);
  }

  .footer__links-group {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .footer__links-group h4 {
    font-family: var(--font-body);
    font-weight: 800;
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--slate-300);
    margin-bottom: var(--space-xs);
  }

  .footer__links-group a {
    font-size: 14px;
    color: var(--slate-400);
    transition: color var(--duration-fast);
  }

  .footer__links-group a:hover {
    color: var(--amber-light);
  }

  .footer__address {
    font-size: 13px;
    color: var(--slate-500);
    line-height: 1.5;
  }

  .footer__bottom {
    margin-top: var(--space-xl);
    padding-top: var(--space-md);
    border-top: 1px solid var(--navy-700);
  }

  .footer__bottom-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer__bottom small {
    font-size: 13px;
    color: var(--slate-500);
  }

  .footer__bottom strong {
    color: var(--slate-300);
    font-weight: 700;
  }

  .footer__back-top {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 13px;
    font-weight: 700;
    color: var(--slate-400);
    transition: color var(--duration-fast);
  }

  .footer__back-top:hover {
    color: var(--amber-light);
  }

  @media (max-width: 768px) {
    .footer__inner {
      flex-direction: column;
      gap: var(--space-xl);
    }

    .footer__links {
      gap: var(--space-xl);
    }

    .footer__bottom-row {
      flex-direction: column;
      gap: var(--space-sm);
      text-align: center;
    }
  }
</style>
