---
const steps = [
  { num: '01', title: 'Cadastrou o Lead', sub: '15 seg · direto do celular' },
  { num: '02', title: 'Ação Definida', sub: 'Automático · próximo passo claro' },
  { num: '03', title: 'Visita Agendada', sub: '1 clique · sincroniza automático', gcal: true },
  { num: '04', title: 'Pipeline na Mão', sub: 'Controle total · toda manhã' },
];
---

<section id="como-funciona" class="section how">
  <div class="container">
    <div class="how__intro">
      <div class="how__header reveal">
        <span class="kicker">Como funciona</span>
        <h2>Do WhatsApp ao<br />Fechamento em 4 passos</h2>
        <p class="how__lead">Feito para não tomar seu tempo. Apenas o essencial para você focar no que dá dinheiro: <strong>vender</strong>.</p>
      </div>
      <div class="how__image reveal reveal-delay-1">
        <div class="how__image-frame">
          <img
            src="/how-it-works.webp"
            alt="Corretor usando o celular na rua em frente a um prédio"
            width="848"
            height="1264"
            loading="lazy"
          />
          <div class="how__image-overlay"></div>
        </div>
      </div>
    </div>

    <div class="how__grid">
      {steps.map((step, i) => (
        <article class={`how__step reveal reveal-delay-${i + 1}`}>
          <span class="how__step-num">{step.num}</span>
          <h3>{step.title}</h3>
          <span class="how__step-sub">
            {step.gcal && <img src="/icons/google-calendar.svg" width="13" height="13" alt="" class="how__gcal-icon" />}
            {step.sub}
          </span>
          {i < steps.length - 1 && <div class="how__step-connector"></div>}
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .how {
    background: var(--navy-900);
    color: var(--text-pure);
    position: relative;
    overflow: hidden;
  }

  .how::before {
     content: '';
     position: absolute;
     inset: 0;
     background: radial-gradient(circle at top right, rgba(221, 148, 41, 0.05) 0%, transparent 50%);
     pointer-events: none;
  }

  /* Intro: header text left + image right */
  .how__intro {
    display: grid;
    grid-template-columns: 1.3fr 1fr;
    gap: var(--space-xl);
    align-items: center;
    margin-bottom: var(--space-lg);
  }

  .how__header {
    max-width: 560px;
  }

  .how__header .kicker {
    color: var(--amber-light);
  }

  .how__header .kicker::before {
     background: var(--amber-light);
  }

  .how__header h2 {
     color: var(--text-pure);
     font-size: 2.4rem;
  }

  .how__lead {
     margin-top: var(--space-md);
     font-size: 1.15rem;
     color: var(--slate-300);
  }

  .how__lead strong {
     color: var(--amber);
  }

  /* Image */
  .how__image-frame {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: 0 24px 48px rgba(0, 0, 0, 0.4);
    max-height: 380px;
  }

  .how__image-frame img {
    display: block;
    width: 100%;
    height: 380px;
    object-fit: cover;
    object-position: 40% 35%;
  }

  .how__image-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, var(--bg-deep) 0%, transparent 40%);
    pointer-events: none;
  }

  /* Steps 4-column grid */
  .how__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 48px;
    position: relative;
  }

  .how__step {
    position: relative;
    padding: 28px 24px 24px;
    background: linear-gradient(
      160deg,
      rgba(17, 24, 39, 0.9) 0%,
      rgba(10, 15, 29, 0.7) 100%
    );
    border: 1px solid rgba(255, 255, 255, 0.04);
    border-radius: 20px;
    transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    overflow: hidden;
  }

  /* Top edge highlight — gives the card a polished "lit edge" feel */
  .how__step::before {
    content: '';
    position: absolute;
    top: 0;
    left: 20%;
    right: 20%;
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(221, 148, 41, 0.4),
      transparent
    );
    transition: all 0.5s ease;
  }

  /* Ambient glow behind card — bloom on hover */
  .how__step::after {
    content: '';
    position: absolute;
    inset: -24px;
    background: radial-gradient(
      circle at 50% 30%,
      rgba(221, 148, 41, 0.07) 0%,
      transparent 70%
    );
    opacity: 0;
    transition: opacity 0.5s ease;
    z-index: -1;
    border-radius: 40px;
    pointer-events: none;
  }

  .how__step:hover {
    transform: translateY(-6px);
    border-color: rgba(221, 148, 41, 0.12);
    box-shadow:
      0 24px 48px rgba(0, 0, 0, 0.4),
      0 0 0 1px rgba(221, 148, 41, 0.06);
  }

  .how__step:hover::before {
    left: 10%;
    right: 10%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(221, 148, 41, 0.6),
      transparent
    );
  }

  .how__step:hover::after {
    opacity: 1;
  }

  /* Number badge — contained, premium feel */
  .how__step-num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 42px;
    height: 42px;
    border-radius: 12px;
    background: rgba(221, 148, 41, 0.06);
    border: 1px solid rgba(221, 148, 41, 0.12);
    font-family: var(--font-display);
    font-size: 0.85rem;
    font-weight: 800;
    color: var(--amber);
    letter-spacing: 0.04em;
    margin-bottom: 20px;
    transition: all 0.4s ease;
  }

  .how__step:hover .how__step-num {
    background: rgba(221, 148, 41, 0.1);
    border-color: rgba(221, 148, 41, 0.25);
    box-shadow: 0 0 20px rgba(221, 148, 41, 0.1);
  }

  .how__step h3 {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.1rem;
    color: var(--text-pure);
    margin-bottom: 6px;
    line-height: 1.3;
  }

  .how__step-sub {
    display: flex;
    align-items: center;
    gap: 5px;
    font-size: 13px;
    font-weight: 500;
    color: var(--text-dim);
    letter-spacing: 0.01em;
  }

  .how__gcal-icon {
    flex-shrink: 0;
  }

  /* Connector — dashed amber line with arrow */
  .how__step-connector {
    position: absolute;
    top: 38px;
    right: -48px;
    width: 48px;
    height: 1px;
    background: repeating-linear-gradient(
      90deg,
      rgba(221, 148, 41, 0.25) 0px,
      rgba(221, 148, 41, 0.25) 4px,
      transparent 4px,
      transparent 10px
    );
    z-index: 10;
  }

  .how__step-connector::after {
    content: '';
    position: absolute;
    right: 0;
    top: -3px;
    width: 7px;
    height: 7px;
    border-right: 1.5px solid rgba(221, 148, 41, 0.5);
    border-top: 1.5px solid rgba(221, 148, 41, 0.5);
    transform: rotate(45deg);
  }

  @media (max-width: 1024px) {
    .how__intro {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .how__header {
      max-width: 640px;
      margin: 0 auto;
    }

    .how__header .kicker {
      justify-content: center;
    }

    .how__image-frame {
      max-height: 360px;
      max-width: 500px;
      margin: 0 auto;
    }

    .how__image-frame img {
      height: 360px;
    }

    .how__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-xl);
    }

    .how__step-connector {
       display: none;
    }
  }

  @media (max-width: 600px) {
    .how__grid {
      grid-template-columns: 1fr;
    }

    .how__image-frame {
      max-height: 280px;
    }

    .how__image-frame img {
      height: 280px;
    }
  }
</style>
