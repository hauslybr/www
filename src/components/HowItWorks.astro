---
const steps = [
  {
    num: '01',
    badge: '15 Segundos',
    title: 'Cadastrou o Lead',
    description: 'Nome, interesse e estágio. Direto do celular, entre uma visita e outra. Sem planilha, sem anotar em guardanapo.',
  },
  {
    num: '02',
    badge: 'Automático',
    title: 'Ação Definida',
    description: 'O Hausly te obriga a definir o próximo passo. Histórico completo de cada contato — nenhum lead esfria no esquecimento.',
  },
  {
    num: '03',
    badge: '1 Clique',
    title: 'Visita Agendada',
    description: 'Link direto pro WhatsApp do cliente com lembrete e endereço do imóvel. Agenda sincronizada com Google Calendar.',
    gcal: true,
  },
  {
    num: '04',
    badge: 'Controle Total',
    title: 'Pipeline na Mão',
    description: 'Bata o olho de manhã e saiba quem cobrar. Seus dados, seus contatos, seus negócios — 100% do corretor.',
  },
];
---

<section id="como-funciona" class="section how">
  <div class="container">
    <div class="how__intro">
      <div class="how__header reveal">
        <span class="kicker">Como funciona</span>
        <h2>Do WhatsApp ao<br />Fechamento em 4 passos</h2>
        <p class="how__lead">Feito para não tomar seu tempo. Apenas o essencial para você focar no que dá dinheiro: <strong>vender</strong>.</p>
      </div>
      <div class="how__image reveal reveal-delay-1">
        <div class="how__image-frame">
          <img
            src="/how-it-works.webp"
            alt="Corretor usando o celular na rua em frente a um prédio"
            width="848"
            height="1264"
            loading="lazy"
          />
          <div class="how__image-overlay"></div>
        </div>
      </div>
    </div>

    <div class="how__grid">
      {steps.map((step, i) => (
        <article class={`how__step reveal reveal-delay-${i + 1}`}>
          <div class="how__step-header">
             <div class="how__step-num">{step.num}</div>
             <span class="how__step-badge">{step.badge}</span>
          </div>
          <div class="how__step-content">
            <h3>{step.title}</h3>
            <p>{step.description}</p>
            {step.gcal && (
              <div class="how__gcal-badge">
                <svg width="16" height="16" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                  <rect x="1" y="4" width="18" height="15" rx="2" fill="#fff"/>
                  <rect x="1" y="4" width="18" height="5" rx="2" fill="#4285f4"/>
                  <rect x="5" y="1" width="2" height="5" rx="1" fill="#4285f4"/>
                  <rect x="13" y="1" width="2" height="5" rx="1" fill="#4285f4"/>
                  <circle cx="6.5" cy="13" r="1.2" fill="#ea4335"/>
                  <circle cx="13.5" cy="13" r="1.2" fill="#ea4335"/>
                  <text x="10" y="16" text-anchor="middle" font-size="7" font-weight="700" fill="#4285f4" font-family="sans-serif">31</text>
                </svg>
                <span>Google Calendar</span>
              </div>
            )}
          </div>
          {i < steps.length - 1 && <div class="how__step-connector"></div>}
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .how {
    background: var(--navy-900);
    color: var(--text-pure);
    position: relative;
    overflow: hidden;
  }

  .how::before {
     content: '';
     position: absolute;
     inset: 0;
     background: radial-gradient(circle at top right, rgba(221, 148, 41, 0.05) 0%, transparent 50%);
     pointer-events: none;
  }

  /* Intro: header text left + image right */
  .how__intro {
    display: grid;
    grid-template-columns: 1.3fr 1fr;
    gap: var(--space-xl);
    align-items: center;
    margin-bottom: var(--space-lg);
  }

  .how__header {
    max-width: 560px;
  }

  .how__header .kicker {
    color: var(--amber-light);
  }

  .how__header .kicker::before {
     background: var(--amber-light);
  }

  .how__header h2 {
     color: var(--text-pure);
     font-size: 2.4rem;
  }

  .how__lead {
     margin-top: var(--space-md);
     font-size: 1.15rem;
     color: var(--slate-300);
  }

  .how__lead strong {
     color: var(--amber);
  }

  /* Image */
  .how__image-frame {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: 0 24px 48px rgba(0, 0, 0, 0.4);
    max-height: 380px;
  }

  .how__image-frame img {
    display: block;
    width: 100%;
    height: 380px;
    object-fit: cover;
    object-position: 40% 35%;
  }

  .how__image-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, var(--bg-deep) 0%, transparent 40%);
    pointer-events: none;
  }

  /* Steps 4-column grid */
  .how__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 48px;
    position: relative;
  }

  .how__step {
    position: relative;
    padding: var(--space-lg);
    background: var(--navy-800);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-lg);
    transition: transform var(--duration-md), box-shadow var(--duration-md), border-color var(--duration-md);
  }

  .how__step:hover {
     transform: translateY(-4px);
     box-shadow: 0 12px 32px rgba(0, 0, 0, 0.3);
     border-color: rgba(221, 148, 41, 0.3);
  }

  .how__step-header {
     display: flex;
     justify-content: space-between;
     align-items: flex-start;
     margin-bottom: var(--space-lg);
  }

  .how__step-num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-size: 2.5rem;
    line-height: 1;
    color: var(--amber);
    opacity: 0.9;
  }

  .how__step-badge {
    display: inline-block;
    padding: 4px 12px;
    background: rgba(221, 148, 41, 0.1);
    border: 1px solid rgba(221, 148, 41, 0.2);
    border-radius: var(--radius-full);
    font-size: 12px;
    font-weight: 800;
    color: var(--amber-light);
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .how__step h3 {
    font-family: var(--font-body);
    font-weight: 700;
    font-size: 1.25rem;
    color: var(--text-pure);
    margin-bottom: var(--space-sm);
    line-height: 1.3;
  }

  .how__step p {
    font-size: 14px;
    color: var(--slate-300);
    line-height: 1.6;
  }

  .how__gcal-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    margin-top: 12px;
    padding: 4px 12px;
    background: rgba(26, 115, 232, 0.1);
    border: 1px solid rgba(26, 115, 232, 0.2);
    border-radius: var(--radius-full);
    font-size: 12px;
    font-weight: 700;
    color: #8ab4f8;
    letter-spacing: 0.02em;
  }

  .how__step-connector {
    position: absolute;
    top: 50%;
    right: -48px;
    width: 48px;
    height: 2px;
    background: rgba(255, 255, 255, 0.1);
    z-index: 10;
    transform: translateY(-50%);
  }

  .how__step-connector::after {
    content: '';
    position: absolute;
    right: 0;
    top: -4px;
    width: 10px;
    height: 10px;
    border-right: 2px solid var(--amber);
    border-top: 2px solid var(--amber);
    transform: rotate(45deg);
  }

  @media (max-width: 1024px) {
    .how__intro {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .how__header {
      max-width: 640px;
      margin: 0 auto;
    }

    .how__header .kicker {
      justify-content: center;
    }

    .how__image-frame {
      max-height: 360px;
      max-width: 500px;
      margin: 0 auto;
    }

    .how__image-frame img {
      height: 360px;
    }

    .how__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-xl);
    }

    .how__step-connector {
       display: none;
    }
  }

  @media (max-width: 600px) {
    .how__grid {
      grid-template-columns: 1fr;
    }

    .how__image-frame {
      max-height: 280px;
    }

    .how__image-frame img {
      height: 280px;
    }
  }
</style>
