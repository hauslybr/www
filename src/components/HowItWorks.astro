---
const steps = [
  {
    num: '01',
    badge: '20s',
    title: 'Cadastre o lead',
    description: 'Nome, telefone, interesse. Direto do celular, entre uma visita e outra.',
  },
  {
    num: '02',
    badge: 'Automático',
    title: 'Próxima ação definida',
    description: 'Nenhum lead fica sem próximo passo. Follow-up com data e lembrete automático.',
  },
  {
    num: '03',
    badge: '1 clique',
    title: 'Agende a visita',
    description: 'Lead, imóvel e horário em um toque. Confirmação automática para você e o cliente.',
  },
  {
    num: '04',
    badge: 'Tempo real',
    title: 'Acompanhe o pipeline',
    description: 'Quanto tem no funil, quantas visitas na semana, quais leads estão quentes. Dados, não chute.',
  },
];
---

<section id="como-funciona" class="section how">
  <div class="container">
    <div class="how__header reveal">
      <span class="kicker">Como funciona</span>
      <h2>De lead a fechamento<br />em 4 passos simples</h2>
    </div>

    <div class="how__grid">
      {steps.map((step, i) => (
        <article class={`how__step reveal reveal-delay-${i + 1}`}>
          <div class="how__step-num">{step.num}</div>
          <div class="how__step-content">
            <span class="how__step-badge">{step.badge}</span>
            <h3>{step.title}</h3>
            <p>{step.description}</p>
          </div>
          {i < steps.length - 1 && <div class="how__step-connector"></div>}
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .how {
    background: var(--surface);
  }

  .how__header {
    text-align: center;
    max-width: 600px;
    margin: 0 auto;
  }

  .how__header .kicker {
    justify-content: center;
  }

  .how__grid {
    margin-top: var(--space-2xl);
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
    position: relative;
  }

  .how__step {
    position: relative;
    text-align: center;
    padding: 0 var(--space-md);
  }

  .how__step-num {
    width: 56px;
    height: 56px;
    margin: 0 auto var(--space-md);
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-lg);
    background: linear-gradient(135deg, var(--amber), #e8a840);
    color: var(--navy-950);
    font-family: var(--font-display);
    font-size: 1.5rem;
    box-shadow: 0 8px 24px rgba(221, 148, 41, 0.25);
  }

  .how__step-badge {
    display: inline-block;
    padding: 3px 10px;
    background: var(--amber-glow);
    border: 1px solid rgba(221, 148, 41, 0.2);
    border-radius: var(--radius-full);
    font-size: 12px;
    font-weight: 800;
    color: var(--amber);
    letter-spacing: 0.03em;
    text-transform: uppercase;
    margin-bottom: var(--space-sm);
  }

  .how__step h3 {
    font-family: var(--font-body);
    font-weight: 800;
    font-size: 1.1rem;
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
    line-height: 1.3;
  }

  .how__step p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.65;
  }

  .how__step-connector {
    position: absolute;
    top: 28px;
    right: -12px;
    width: 24px;
    height: 2px;
    background: var(--border);
    z-index: 1;
  }

  .how__step-connector::after {
    content: '';
    position: absolute;
    right: -2px;
    top: -3px;
    width: 8px;
    height: 8px;
    border-right: 2px solid var(--amber);
    border-top: 2px solid var(--amber);
    transform: rotate(45deg);
  }

  @media (max-width: 900px) {
    .how__grid {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }

    .how__step {
      text-align: left;
      display: grid;
      grid-template-columns: 56px 1fr;
      gap: var(--space-md);
      padding: 0;
    }

    .how__step-num {
      margin: 0;
    }

    .how__step-connector {
      display: none;
    }
  }
</style>
