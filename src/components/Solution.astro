---
const before = [
  'Lead chega no fim de semana e esfria até segunda',
  'Visita agendada de cabeça, cliente esqueceu',
  'Fim de mês sem saber exatamente quanto tem no funil',
  'Rotina caótica entre WhatsApp, Google Drive e memória',
];

const after = [
  'Controle total: próxima ação definida e agendada',
  'Agenda sincronizada e lembretes por WhatsApp',
  'Pipeline visual: saiba exatamente quem cobrar hoje',
  'Único fluxo: do "Oi" no WhatsApp ao PIX da comissão',
];
---

<section id="solucao" class="section solution">
  <div class="container">
    <div class="solution__header reveal">
      <span class="kicker">A transformação</span>
      <h2>Antes: correndo atrás.<br />Depois: no controle absoluto.</h2>
    </div>

    <div class="solution__grid">
      <article class="solution__panel solution__panel--before reveal reveal-delay-1">
        <div class="solution__panel-tag solution__panel-tag--red">Antes</div>
        <ul>
          {before.map((item) => (
            <li>
              <svg width="18" height="18" viewBox="0 0 18 18" fill="none"><circle cx="9" cy="9" r="8" stroke="#ef4444" stroke-width="1.5"/><path d="M6 6l6 6M12 6l-6 6" stroke="#ef4444" stroke-width="1.5" stroke-linecap="round"/></svg>
              {item}
            </li>
          ))}
        </ul>
      </article>

      <div class="solution__arrow reveal reveal-delay-2">
        <svg width="40" height="40" viewBox="0 0 40 40" fill="none"><circle cx="20" cy="20" r="19" stroke="var(--amber)" stroke-width="2"/><path d="M14 20h12m0 0l-5-5m5 5l-5 5" stroke="var(--amber)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </div>

      <article class="solution__panel solution__panel--after reveal reveal-delay-3">
        <div class="solution__panel-tag solution__panel-tag--green">Com Hausly</div>
        <ul>
          {after.map((item) => (
            <li>
              <svg width="18" height="18" viewBox="0 0 18 18" fill="none"><circle cx="9" cy="9" r="8" stroke="#22c55e" stroke-width="1.5"/><path d="M6 9l2.5 2.5L12 7" stroke="#22c55e" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
              {item}
            </li>
          ))}
        </ul>
      </article>
    </div>
  </div>
</section>

<style>
  .solution {
    background: var(--navy-900);
    color: var(--text-pure);
    overflow: hidden;
  }

  .solution .kicker {
    color: var(--amber-light);
  }

  .solution .kicker::before {
    background: var(--amber-light);
  }

  .solution h2 {
    color: var(--text-pure);
  }

  .solution__grid {
    margin-top: var(--space-xl);
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: var(--space-lg);
    align-items: center;
  }

  .solution__panel {
    padding: var(--space-lg);
    border-radius: var(--radius-lg);
    border: 1px solid var(--navy-600);
    background: var(--navy-800);
  }

  .solution__panel ul {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .solution__panel li {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 15px;
    font-weight: 500;
    color: var(--slate-300);
    line-height: 1.4;
  }

  .solution__panel li svg {
    flex-shrink: 0;
  }

  .solution__panel-tag {
    display: inline-block;
    padding: 4px 14px;
    border-radius: var(--radius-full);
    font-size: 12px;
    font-weight: 800;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    margin-bottom: var(--space-md);
  }

  .solution__panel-tag--red {
    background: rgba(239, 68, 68, 0.15);
    color: #fca5a5;
  }

  .solution__panel-tag--green {
    background: rgba(34, 197, 94, 0.15);
    color: #86efac;
  }

  .solution__arrow {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  @media (max-width: 768px) {
    .solution__grid {
      grid-template-columns: 1fr;
      gap: var(--space-md);
    }

    .solution__arrow {
      transform: rotate(90deg);
    }
  }
</style>
