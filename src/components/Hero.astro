---

---

<section id="inicio" class="hero noise">
  <div class="hero__bg-shapes">
    <div class="hero__orb hero__orb--amber"></div>
    <div class="hero__orb hero__orb--blue"></div>
  </div>

  <div class="container hero__grid">
    <div class="hero__content">
      <div class="hero__badge reveal">
        <span class="hero__badge-dot"></span>
        CRM feito por quem entende a rotina do corretor
      </div>

      <h1 class="hero__title reveal reveal-delay-1">
        Sua semana virou<br />
        <span class="hero__title-accent">um plano executável.</span>
      </h1>

      <p class="hero__subtitle reveal reveal-delay-2">
        Lead sumindo no WhatsApp, planilha abandonada, visita que fura.<br />
        A Hausly organiza funil, agenda e follow-up em um lugar só.
      </p>

      <div class="hero__stores reveal reveal-delay-3">
        <p class="hero__coming">Em breve na App Store e Google Play</p>
        <div class="hero__store-badges">
          <div class="hero__store-badge">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M17.05 20.28c-.98.95-2.05.8-3.08.35-1.09-.46-2.09-.48-3.24 0-1.44.62-2.2.44-3.06-.35C2.79 15.25 3.51 7.59 9.05 7.31c1.35.07 2.29.74 3.08.8 1.18-.24 2.31-.93 3.57-.84 1.51.12 2.65.72 3.4 1.8-3.12 1.87-2.38 5.98.48 7.13-.57 1.5-1.31 2.99-2.54 4.09zM12.03 7.25c-.15-2.23 1.66-4.07 3.74-4.25.29 2.58-2.34 4.5-3.74 4.25z" fill="currentColor"/></svg>
            App Store
          </div>
          <div class="hero__store-badge">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M3.18 23.69c-.56-.29-.97-.81-.97-1.63V1.94c0-.82.41-1.34.97-1.63L13.56 12 3.18 23.69zm1.44-24.2L16.22 6.3l-2.82 2.82L4.62-.51zm0 25.02l8.78-8.61 2.82 2.82L4.62 24.51zM20.16 10.12c.7.38 1.17 1.04 1.17 1.88s-.47 1.5-1.17 1.88l-3.2 1.76-3.1-3.04 3.1-3.04 3.2 1.56z" fill="currentColor"/></svg>
            Google Play
          </div>
        </div>
      </div>

      <p class="hero__trust reveal reveal-delay-4">
        Grátis até 25 leads &middot; Sem cartão &middot; App nativo iOS e Android
      </p>
    </div>

    <div class="hero__image reveal reveal-delay-2">
      <div class="hero__image-frame">
        <img
          src="/hero-broker.webp"
          alt="Corretor de imóveis usando a Hausly no celular em frente a um prédio moderno"
          width="1408"
          height="768"
          loading="eager"
        />
        <div class="hero__image-gradient"></div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: 80px 0 100px;
    overflow: hidden;
  }

  .hero__bg-shapes {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 0;
  }

  .hero__orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
  }

  .hero__orb--amber {
    width: 600px;
    height: 600px;
    top: -200px;
    right: -100px;
    background: radial-gradient(circle, rgba(221, 148, 41, 0.12) 0%, transparent 70%);
  }

  .hero__orb--blue {
    width: 500px;
    height: 500px;
    bottom: -150px;
    left: -100px;
    background: radial-gradient(circle, rgba(59, 130, 246, 0.08) 0%, transparent 70%);
  }

  .hero__grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: var(--space-2xl);
    align-items: center;
  }

  .hero__badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 16px 6px 10px;
    background: var(--amber-glow);
    border: 1px solid rgba(221, 148, 41, 0.2);
    border-radius: var(--radius-full);
    font-size: 13px;
    font-weight: 700;
    color: var(--amber);
    letter-spacing: 0.02em;
    text-transform: uppercase;
    margin-bottom: var(--space-lg);
    width: fit-content;
  }

  .hero__badge-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--amber);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.4); }
  }

  .hero__title {
    line-height: 1.05;
    letter-spacing: -0.03em;
    color: var(--text-primary);
  }

  .hero__title-accent {
    color: var(--amber);
    font-style: italic;
  }

  .hero__subtitle {
    margin-top: var(--space-lg);
    font-size: clamp(1.05rem, 1.8vw, 1.25rem);
    line-height: 1.65;
    color: var(--text-secondary);
    max-width: 540px;
  }

  .hero__stores {
    margin-top: var(--space-xl);
  }

  .hero__coming {
    font-size: 14px;
    font-weight: 700;
    color: var(--amber);
    margin-bottom: var(--space-sm);
  }

  .hero__store-badges {
    display: flex;
    gap: 12px;
    margin-top: 8px;
  }

  .hero__store-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 18px;
    background: var(--navy-900);
    color: var(--white);
    border-radius: var(--radius-md);
    font-size: 14px;
    font-weight: 600;
  }

  .hero__store-badge svg {
    flex-shrink: 0;
  }

  .hero__trust {
    margin-top: var(--space-md);
    font-size: 13px;
    font-weight: 600;
    color: var(--text-muted);
  }

  /* Hero image */
  .hero__image {
    display: flex;
    justify-content: center;
  }

  .hero__image-frame {
    position: relative;
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow:
      var(--shadow-lg),
      0 32px 64px rgba(0, 0, 0, 0.08);
  }

  .hero__image-frame img {
    display: block;
    width: 100%;
    height: auto;
  }

  .hero__image-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to top,
      rgba(250, 249, 247, 0.5) 0%,
      transparent 25%
    );
    pointer-events: none;
  }

  @media (max-width: 900px) {
    .hero {
      padding: 48px 0 64px;
    }

    .hero__grid {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }
  }
</style>
